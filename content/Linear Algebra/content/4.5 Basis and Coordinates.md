If $S = \{v_{1}, v_{2}, v_{3}, \dots, v_{n}$ is a set of vectors from the vector space $V$. Then S is called a basis for $V$ if both the following conditions hold: 
1) $S$ spans vector space $V$. 
2) $S$ is linearly independent.