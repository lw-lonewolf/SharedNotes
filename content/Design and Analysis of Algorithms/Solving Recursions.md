$$T(n) = aT\left( \frac{n}{b} \right)+f(n)$$
$a$ = number of recursive calls 
$b$ = factor by which the problem gets reduced at each level of recursion
f(n) = work done outside recursion 


#### Work at Level $i$
