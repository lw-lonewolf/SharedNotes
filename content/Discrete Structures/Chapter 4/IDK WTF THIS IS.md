d|a and d|b 

d -> GCD (if d is the largest such number)

gcd(a,b) = d

Integers $a$ and $b$ are **relatively prime** if their greatest gcd is 1. 

ab =gcd(a,b) * lcm(a,b)

# The Euclidean Algorithm: 
If $a = bq+r$
$gcd(a,b) = gcd(b,r)$

# Bezout's Identity: 

gcd(a,b) = x
we can write this as 
gcd(a,b) = sa + bt
s,t are BC.

# Inverse modulo m: 
$a\cdot \bar{a} =1$ (mod m)